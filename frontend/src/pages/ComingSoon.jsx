import React from "react";
const ComingSoon = ({ label }) => (
    <div className="container" style={{ paddingTop: 60 }}>
        <p style={{ color: 'var(--text-dim)' }}>{label} — built in the next step.</p>
    </div>
);

export default ComingSoon;